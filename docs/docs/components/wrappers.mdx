# Wrappers

### TextRequestsWrapper

Lightweight wrapper around requests library.

**TODO: Why, when and where is it used?**

**Params**

- **header:** Component of the HTTP request that contains additional information about the client or the requested resource. Defaults to `{'Authorization': 'Bearer <token>'}`
