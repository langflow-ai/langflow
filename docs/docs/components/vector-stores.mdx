import Admonition from '@theme/Admonition';

# Vector Stores

<Admonition type="caution" icon="🚧" title="ZONE UNDER CONSTRUCTION">
    <p>
        We appreciate your understanding as we polish our documentation – it may contain some rough edges. Share your feedback or report issues to help us improve! 🛠️📝
    </p>
</Admonition>


### Chroma

The `Chroma` is a component designed for implementing a Vector Store using Chroma. This component allows users to utilize Chroma for efficient vector storage and retrieval within their language processing workflows.

For detailed documentation and integration guides, please refer to the [Chroma Component Documentation](https://python.langchain.com/docs/integrations/vectorstores/chroma).


**Configuration Options**

The `Chroma` offers various configuration options to customize its behavior. Here are the available options:

- **Collection Name**: The name of the collection.
- **Persist Directory**: The directory to persist the Vector Store to.
- **Code**: Advanced option for code settings.
- **Input**: Specifies the type of input (Document or Record).
- **Embedding**: Specifies the embedding to use for the Vector Store.
- **Server CORS Allow Origins**: CORS allow origins for the Chroma server.
- **Server Host**: The host for the Chroma server.
- **Server Port**: The port for the Chroma server.
- **Server gRPC Port**: The gRPC port for the Chroma server.
- **Server SSL Enabled**: Whether to enable SSL for the Chroma server.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store or BaseRetriever object.

**Arguments**

- `collection_name (str)`: The name of the collection.
- `index_directory (Optional[str])`: The directory to persist the Vector Store to.
- `chroma_server_ssl_enabled (bool)`: Whether to enable SSL for the Chroma server.
- `embedding (Optional[Embeddings])`: The embeddings to use for the Vector Store.
- `inputs (Optional[List[Record]])`: The inputs to use for the Vector Store.
- `chroma_server_cors_allow_origins (Optional[str])`: The CORS allow origins for the Chroma server.
- `chroma_server_host (Optional[str])`: The host for the Chroma server.
- `chroma_server_port (Optional[int])`: The port for the Chroma server.
- `chroma_server_grpc_port (Optional[int])`: The gRPC port for the Chroma server.

**Returns**

- `Union[VectorStore, BaseRetriever]`: The Vector Store or BaseRetriever object.

---

### Chroma Search


The `ChromaSearch` is a component designed for searching a Chroma collection for similar documents. This component integrates with Chroma to facilitate efficient document retrieval based on similarity metrics.

The `ChromaSearch` provides several configuration options to customize its behavior:

- **Input**: The input text for the search.
- **Search Type**: Specifies the type of search (Similarity or MMR).
- **Collection Name**: The name of the collection.
- **Index Directory**: The directory where the index is stored.
- **Embedding**: The embedding model used to vectorize inputs.
- **Server CORS Allow Origins**: CORS allow origins for the Chroma server.
- **Server Host**: The host for the Chroma server.
- **Server Port**: The port for the Chroma server.
- **Server gRPC Port**: The gRPC port for the Chroma server.
- **Server SSL Enabled**: Whether to enable SSL for the Chroma server.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store or BaseRetriever object.

**Arguments**

- `input_value (Text)`: The input text for the search.
- `search_type (str)`: The type of search (Similarity or MMR).
- `collection_name (str)`: The name of the collection.
- `embedding (Embeddings)`: The embedding model used to vectorize inputs.
- `chroma_server_ssl_enabled (bool)`: Whether to enable SSL for the Chroma server.
- `index_directory (Optional[str])`: The directory where the index is stored.
- `chroma_server_cors_allow_origins (Optional[str])`: CORS allow origins for the Chroma server.
- `chroma_server_host (Optional[str])`: The host for the Chroma server.
- `chroma_server_port (Optional[int])`: The port for the Chroma server.
- `chroma_server_grpc_port (Optional[int])`: The gRPC port for the Chroma server.

**Returns**

- `List[Record]`: A list of records representing the search results.

---

### FAISS 

The `FAISS` is a component designed for ingesting documents into a FAISS Vector Store. It facilitates efficient document indexing and retrieval using the FAISS library.

For detailed documentation and integration guides, please refer to the [FAISS Component Documentation](https://faiss.ai/index.html).

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store or BaseRetriever object.

**Arguments**

- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `inputs (List[Record])`: The inputs to ingest into FAISS.
- `folder_path (str)`: The path to save the FAISS index. It will be relative to where Langflow is running.
- `index_name (str, optional)`: The name of the FAISS index. Default is "langflow_index".

**Returns**

- `Union[VectorStore, FAISS, BaseRetriever]`: The Vector Store, FAISS, or BaseRetriever object.

---

### FAISS Search

The `FAISSSearch` is a component for searching a FAISS Vector Store for similar documents. It enables efficient document retrieval based on similarity metrics using FAISS.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store or BaseRetriever object.

**Arguments**

- `input_value (Text)`: The input text for the search.
- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `folder_path (str)`: The path where the FAISS index is saved. Relative to where Langflow is running.
- `index_name (str, optional)`: The name of the FAISS index. Default is "langflow_index".

**Returns**

- `List[Record]`: A list of records representing the search results.

---


### MongoDB Atlas

The `MongoDBAtlas` is a component used to construct a MongoDB Atlas Vector Search vector store from raw documents. It facilitates the creation of MongoDB Atlas-based vector stores for efficient document storage and retrieval.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the MongoDB Atlas Vector Search object.

**Arguments**

- `embedding (Embeddings)`: The embeddings to use for the vector store.
- `inputs (Optional[List[Record]])`: The input records or documents.
- `collection_name (str)`: The name of the collection in the database.
- `db_name (str)`: The name of the database.
- `index_name (str)`: The name of the index.
- `mongodb_atlas_cluster_uri (str)`: The URI for the MongoDB Atlas cluster.
- `search_kwargs (Optional[NestedDict])`: Additional search arguments (advanced).

**Returns**

- `MongoDBAtlasVectorSearch`: The MongoDB Atlas Vector Search object.

<Admonition type="note" title="Note">
    <p>
        Ensure that pymongo is installed to use MongoDB Atlas Vector Store.
    </p>
</Admonition>

---

### MongoDB Atlas Search

The `MongoDBAtlasSearch` is a component for searching a MongoDB Atlas Vector Store for similar documents. It extends the functionality of the MongoDBAtlasComponent to provide efficient document retrieval based on similarity metrics.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the MongoDB Atlas Vector Search object and performs the search operation.

**Arguments**

- `input_value (Text)`: The input text for the search.
- `search_type (str)`: The type of search (Similarity or MMR).
- `embedding (Embeddings)`: The embeddings to use for the vector store.
- `collection_name (str)`: The name of the collection in the database.
- `db_name (str)`: The name of the database.
- `index_name (str)`: The name of the index.
- `mongodb_atlas_cluster_uri (str)`: The URI for the MongoDB Atlas cluster.
- `search_kwargs (Optional[NestedDict])`: Additional search arguments (advanced).

**Returns**

- `List[Record]`: A list of records representing the search results.

---

### PGVector 

The `PGVector` is a component for implementing a Vector Store using PostgreSQL. It allows users to store and retrieve vectors efficiently within a PostgreSQL database.

For detailed documentation and integration guides, please refer to the [PGVector Component Documentation](https://python.langchain.com/docs/integrations/vectorstores/pgvector).

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store object.

**Arguments**

- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `pg_server_url (str)`: The connection string for the PostgreSQL server.
- `collection_name (str)`: The name of the table in PostgreSQL.
- `inputs (Optional[Record])`: The input records or documents.

**Returns**

- `Union[VectorStore, BaseRetriever]`: The Vector Store or BaseRetriever object.

<Admonition type="note" title="Note">
    <p>
        Ensure that the required PostgreSQL server is accessible and properly configured.
    </p>
</Admonition>

---

### PGVector Search

The `PGVectorSearch` is a component for searching a PGVector Store for similar documents. It extends the functionality of the PGVectorComponent to provide efficient document retrieval based on similarity metrics.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store object and performs the search operation.

**Arguments**

- `input_value (Text)`: The input text value to search for.
- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `search_type (str)`: The type of search (Similarity or MMR).
- `pg_server_url (str)`: The connection string for the PostgreSQL server.
- `collection_name (str)`: The name of the table in PostgreSQL.

**Returns**

- `List[Record]`: A list of records representing the search results.

---

### Pinecone

**Overview**

The `Pinecone` is a component used to construct a Pinecone wrapper from raw documents. It facilitates the creation of Pinecone-based vector indexes for efficient document storage and retrieval.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Pinecone Vector Store object.

**Arguments**

- `embedding (Embeddings)`: The embeddings to use for the vector store.
- `pinecone_env (str)`: The environment for Pinecone.
- `inputs (Optional[List[Record]])`: The input records or documents.
- `text_key (str)`: The key for the text content in the documents.
- `pool_threads (int)`: The number of threads to use for parallel operations.
- `index_name (Optional[str])`: The name of the index in Pinecone.
- `pinecone_api_key (Optional[str])`: The API key for Pinecone.
- `namespace (Optional[str])`: The namespace for the index.

**Returns**

- `Union[VectorStore, Pinecone, BaseRetriever]`: The Vector Store, Pinecone, or BaseRetriever object.

<Admonition type="note" title="Note">
    <p>
        Ensure that the required Pinecone API key and environment are properly configured.
    </p>
</Admonition>

---

### Pinecone Search

The `PineconeSearch` is a component used to search a Pinecone Vector Store for similar documents. It extends the functionality of the `PineconeComponent` to provide efficient document retrieval based on similarity metrics.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store object and performs the search operation.

**Arguments**

- `input_value (Text)`: The input text value to search for.
- `embedding (Embeddings)`: The embeddings to use for the vector store.
- `pinecone_env (str)`: The environment for Pinecone.
- `text_key (str)`: The key for the text content in the documents.
- `pool_threads (int)`: The number of threads to use for parallel operations.
- `index_name (Optional[str])`: The name of the index in Pinecone.
- `pinecone_api_key (Optional[str])`: The API key for Pinecone.
- `namespace (Optional[str])`: The namespace for the index.
- `search_type (str)`: The type of search (Similarity or MMR).

**Returns**

- `List[Record]`: A list of records representing the search results.

---

### Qdrant 

The `Qdrant` is a component used to construct a Qdrant wrapper from a list of texts. It allows for efficient similarity search and retrieval operations based on the provided embeddings.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store object and performs the search operation.

**Arguments**

- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `collection_name (str)`: The name of the collection in Qdrant.
- `inputs (Optional[Record])`: The input documents or records.
- `api_key (Optional[str])`: The API key for accessing Qdrant (optional).
- `content_payload_key (str)`: The key for the content payload in the documents.
- `distance_func (str)`: The distance function to use (default: Cosine).
- `grpc_port (int)`: The gRPC port for communication with Qdrant.
- `https (bool)`: Whether to use HTTPS for communication.
- `host (Optional[str])`: The host URL for Qdrant.
- `location (Optional[str])`: The location of the Qdrant service.
- `metadata_payload_key (str)`: The key for the metadata payload in the documents.
- `path (Optional[str])`: The path for the Qdrant service.
- `port (Optional[int])`: The port for communication with Qdrant.
- `prefer_grpc (bool)`: Whether to prefer gRPC communication.
- `prefix (Optional[str])`: The prefix for the Qdrant service.
- `search_kwargs (Optional[NestedDict])`: Additional search keyword arguments.
- `timeout (Optional[int])`: The timeout for communication with Qdrant.
- `url (Optional[str])`: The URL for communication with Qdrant.

**Returns**

- `Union[VectorStore, Qdrant, BaseRetriever]`: The constructed Vector Store or Qdrant object.

<Admonition type="note" title="Note">
    <p>
        Ensure that the required Qdrant service parameters are properly configured.
    </p>
</Admonition>

---

### Qdrant Search

The `QdrantSearch` is a component used to search a Qdrant Vector Store for similar documents. It extends the functionality of the `QdrantComponent` to provide efficient document retrieval based on similarity metrics.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build()`****

Builds the Vector Store object and performs the search operation.

**Arguments**

- `input_value (Text)`: The input text value to search for.
- `embedding (Embeddings)`: The embeddings to use for the vector store.
- `collection_name (str)`: The name of the collection in Qdrant.
- `search_type (str)`: The type of search (Similarity or MMR).
- `api_key (Optional[str])`: The API key for Qdrant.
- `content_payload_key (str)`: The key for the content payload in the documents.
- `distance_func (str)`: The distance function to use.
- `grpc_port (int)`: The gRPC port for communication.
- `https (bool)`: Whether to use HTTPS or not.
- `host (Optional[str])`: The host address.
- `location (Optional[str])`: The location for Qdrant.
- `metadata_payload_key (str)`: The key for the metadata payload in the documents.
- `path (Optional[str])`: The path for Qdrant.
- `port (Optional[int])`: The port for communication.
- `prefer_grpc (bool)`: Whether to prefer gRPC or not.
- `prefix (Optional[str])`: The prefix for Qdrant.
- `search_kwargs (Optional[NestedDict])`: Additional search keyword arguments.
- `timeout (Optional[int])`: The timeout value.
- `url (Optional[str])`: The URL for Qdrant.

**Returns**

- `List[Record]`: A list of records representing the search results.

---

### Redis

The `Redis` is a component for implementing a Vector Store using Redis. It provides functionality to store and retrieve vectors efficiently from a Redis database.

For detailed documentation, please refer to the [Redis Documentation](https://python.langchain.com/docs/integrations/vectorstores/redis).

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build(embedding: Embeddings, redis_server_url: str, redis_index_name: str, schema: Optional[str] = None, inputs: Optional[Record] = None) -> Union[VectorStore, BaseRetriever]`****

Builds the Vector Store or BaseRetriever object.

**Arguments**

- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `redis_server_url (str)`: The URL for the Redis server.
- `redis_index_name (str)`: The name of the Redis index.
- `schema (Optional[str])`: The schema for the documents (required if no documents are provided).
- `inputs (Optional[Record])`: The documents to use for the Vector Store.

**Returns**

- `Union[VectorStore, BaseRetriever]`: The Vector Store object or BaseRetriever object.

<Admonition type="note" title="Note">
    <p>
        Ensure that the required Redis server connection URL and index name are properly configured. If no documents are provided, a schema must be provided.
    </p>
</Admonition>

---

### Redis Search

The `RedisSearch` is a component for searching a Redis Vector Store for similar documents.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build(input_value: Text, search_type: str, embedding: Embeddings, redis_server_url: str, redis_index_name: str, schema: Optional[str] = None) -> List[Record]`****

Builds the Vector Store object and performs the search operation.

**Arguments**

- `input_value (Text)`: The input value to search for.
- `search_type (str)`: The type of search to perform.
- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `redis_server_url (str)`: The URL for the Redis server.
- `redis_index_name (str)`: The name of the Redis index.
- `schema (Optional[str])`: The schema for the documents (required if no documents are provided).

**Returns**

- `List[Record]`: The list of records containing similar documents.

---

### Supabase

The `Supabase` is a component for initializing a Supabase Vector Store from texts and embeddings.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build(embedding: Embeddings, inputs: Optional[List[Record]] = None, query_name: str = "", search_kwargs: NestedDict = {}, supabase_service_key: str = "", supabase_url: str = "", table_name: str = "") -> Union[VectorStore, SupabaseVectorStore, BaseRetriever]`****

Builds the Vector Store object initialized from texts and embeddings.

**Arguments**

- `embedding (Embeddings)`: The embeddings to use for the Vector Store.
- `inputs (Optional[List[Record]])`: The list of records or documents to use for initializing the Vector Store.
- `query_name (str)`: The name of the query.
- `search_kwargs (NestedDict)`: Additional keyword arguments for search operations (optional).
- `supabase_service_key (str)`: The Supabase service key.
- `supabase_url (str)`: The URL for the Supabase instance.
- `table_name (str)`: The name of the table in the Supabase instance.

**Returns**

- `Union[VectorStore, SupabaseVectorStore, BaseRetriever]`: The initialized Vector Store object.

<Admonition type="note" title="Note">
    <p>
        Ensure that the required Supabase service key, Supabase URL, and table name are properly configured.
    </p>
</Admonition>

---

### Supabase Search

The `SupabaseSearch` is a component for searching a Supabase Vector Store for similar documents.

**Methods**

****`build_config()`****

Builds the configuration for the component.

**Returns**

- `dict`: A dictionary containing the configuration options for the component.

****`build(input_value: Text, search_type: str, embedding: Embeddings, query_name: str = "", supabase_service_key: str = "", supabase_url: str = "", table_name: str = "") -> List[Record]`****

Searches for similar documents in the Supabase Vector Store.

**Arguments**

- `input_value (Text)`: The input text to search for.
- `search_type (str)`: The type of search (e.g., "Similarity" or "MMR").
- `embedding (Embeddings)`: The embeddings to use for the search.
- `query_name (str)`: The name of the query (optional).
- `supabase_service_key (str)`: The Supabase service key.
- `supabase_url (str)`: The URL for the Supabase instance.
- `table_name (str)`: The name of the table in the Supabase instance.

**Returns**

- `List[Record]`: A list of records representing the similar documents found in the Supabase Vector Store.

---

### Vectara

The `Vectara` is a component for implementing a Vector Store using Vectara.

For detailed documentation and integration guides, please refer to the [Vectara Component Documentation](https://python.langchain.com/docs/integrations/vectorstores/vectara).


**Methods**

****`build(vectara_customer_id: str, vectara_corpus_id: str, vectara_api_key: str, files_url: Optional[List[str]] = None, inputs: Optional[Record] = None) -> Union[VectorStore, BaseRetriever]`****

Builds the Vector Store or BaseRetriever object.

**Arguments**

- `vectara_customer_id (str)`: The customer ID for Vectara.
- `vectara_corpus_id (str)`: The corpus ID for Vectara.
- `vectara_api_key (str)`: The API key for Vectara.
- `files_url (Optional[List[str]])`: The URLs of files to be processed by Vectara (optional).
- `inputs (Optional[Record])`: Input records or documents to be processed by Vectara (optional).

**Returns**

- `Union[VectorStore, BaseRetriever]`: The Vector Store or BaseRetriever object.

<Admonition type="note" title="Note">
    <p>
        If `inputs` are provided, they will be upserted to the corpus. If `files_url` are provided, Vectara will process the files from the URLs.
    </p>
</Admonition>

---

### Vectara Search

The `VectaraSearch` is a component for searching a Vectara Vector Store for similar documents.

**Methods**

****`build(input_value: Text, search_type: str, vectara_customer_id: str, vectara_corpus_id: str, vectara_api_key: str) -> List[Record]`****

Searches the Vectara Vector Store for similar documents.

**Arguments**

- `input_value (Text)`: The input text to search for similarities.
- `search_type (str)`: The type of search to perform (options: "Similarity", "MMR").
- `vectara_customer_id (str)`: The customer ID for Vectara.
- `vectara_corpus_id (str)`: The corpus ID for Vectara.
- `vectara_api_key (str)`: The API key for Vectara.

**Returns**

- `List[Record]`: A list of records representing similar documents found in the Vectara Vector Store.

**Field Configuration**

- `search_type`: Display name: Search Type, Options: ["Similarity", "MMR"].
- `input_value`: Display name: Input.
- `vectara_customer_id`: Display name: Vectara Customer ID.
- `vectara_corpus_id`: Display name: Vectara Corpus ID.
- `vectara_api_key`: Display name: Vectara API Key, Password: True.
- `files_url`: Display name: Files URL, Info: Make vectara object using URL of files (optional).

---

### Weaviate

The `Weaviate` is a component for implementing a Vector Store using Weaviate.

For detailed documentation and integration guides, please refer to the [Weaviate Component Documentation](https://python.langchain.com/docs/integrations/vectorstores/weaviate).


**Methods**

****`build(url: str, search_by_text: bool = False, api_key: Optional[str] = None, index_name: Optional[str] = None, text_key: str = "text", embedding: Optional[Embeddings] = None, inputs: Optional[Record] = None, attributes: Optional[list] = None) -> Union[VectorStore, BaseRetriever]`****

Builds the Vector Store or BaseRetriever object using Weaviate.

**Arguments**

- `url (str)`: The URL of the Weaviate instance.
- `search_by_text (bool)`: Whether to enable text-based search.
- `api_key (Optional[str])`: The API key for authentication (optional).
- `index_name (Optional[str])`: The name of the index (optional).
- `text_key (str)`: The key representing text data in documents (default: "text").
- `embedding (Optional[Embeddings])`: The embeddings to use for the Vector Store (optional).
- `inputs (Optional[Record])`: The documents to use for the Vector Store (optional).
- `attributes (Optional[list])`: Additional attributes to consider (optional).

**Returns**

- `Union[VectorStore, BaseRetriever]`: The Vector Store or BaseRetriever object.

**Field Configuration**

- `url`: Display name: Weaviate URL, Value: "http://localhost:8080".
- `api_key`: Display name: API Key, Password: True, Required: False.
- `index_name`: Display name: Index name, Required: False.
- `text_key`: Display name: Text Key, Value: "text", Required: False, Advanced: True.
- `inputs`: Display name: Input, Input Types: ["Document", "Record"].
- `embedding`: Display name: Embedding, Required: False.
- `attributes`: Display name: Attributes, Required: False, Is List: True, Field Type: "str", Advanced: True.
- `search_by_text`: Display name: Search By Text, Field Type: "bool", Advanced: True.
- `code`: Show: False.

<Admonition type="note" title="Note">
    <p>
        Before using the Weaviate Vector Store component, ensure that you have a Weaviate instance running and accessible at the specified URL. Additionally, make sure to provide the correct API key for authentication if required. Adjust the index name, text key, and attributes according to your dataset and indexing requirements. Finally, ensure that the provided embeddings are compatible with Weaviate's requirements.
    </p>
</Admonition>

---

### Weaviate Search

The `WeaviateSearch` component facilitates searching a Weaviate Vector Store for similar documents.

**Configuration**

The `WeaviateSearch` component can be configured using the following parameters:

- **Search Type**: Specifies the type of search operation to perform. Options include "Similarity" and "MMR".
- **Input**: The input text utilized for the search operation.
- **Weaviate URL**: URL of the Weaviate server. Default is "http://localhost:8080".
- **API Key**: (Optional) API key required for authentication with the Weaviate server.
- **Index Name**: (Optional) Name of the index to search within the Weaviate server.
- **Text Key**: (Optional) Key representing the text field in the indexed documents. Default is "text".
- **Embedding**: The embedding used for representing text documents.
- **Attributes**: (Optional) Additional attributes considered during the search operation.
- **Search By Text**: (Optional) When enabled, the search operation is performed based on the input text.
- **Code**: (Hidden) This parameter is not visible in the configuration.

**Build Method**

The `build` method of the `WeaviateSearchVectorStore` component constructs the search functionality. It accepts the following parameters:

- **input_value**: The input text for the search operation.
- **search_type**: Specifies the type of search operation, either "Similarity" or "MMR".
- **url**: The URL of the Weaviate server.
- **search_by_text**: (Optional) If set to `True`, the search operation is performed based on the input text.
- **api_key**: (Optional) API key for authentication with the Weaviate server.
- **index_name**: (Optional) Name of the index to search within the Weaviate server.
- **text_key**: (Optional) Key representing the text field in the indexed documents.
- **embedding**: The embedding used for representing text documents.
- **attributes**: (Optional) Additional attributes considered during the search operation.

**Return Value**

The `build` method returns a list of records containing documents similar to the input text based on the specified search type. If the Weaviate index fails to load, a `ValueError` is raised.