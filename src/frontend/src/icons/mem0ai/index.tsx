import React, { forwardRef } from "react";
import SvgMem0 from "./mem0";

export const Mem0Icon = forwardRef<SVGSVGElement, React.PropsWithChildren<{}>>(
  (props, ref) => {
    return <SvgMem0 ref={ref} {...props} />;
  },
);
