import Loading from "../../../ui/loading";

export default function LoadingOverlay() {
  return (
    <div className="bg-background flex h-full w-full items-center justify-center align-middle">
      <Loading />
    </div>
  );
}
