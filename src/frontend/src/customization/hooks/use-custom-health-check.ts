export function useCustomHealthCheck() {
  const message = "";
  const description = "";
  const isFetching = false;
  const isError = false;

  return {
    message,
    description,
    isFetching,
    isError,
  };
}
