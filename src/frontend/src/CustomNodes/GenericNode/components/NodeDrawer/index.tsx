const NodeDrawer = ({
  onClose,
  open,
}: {
  onClose: () => void;
  open: boolean;
}) => {
  return (
    <div>
      <div>
        <p>Node Settings Panel</p>
      </div>
    </div>
  );
};

export default NodeDrawer;
