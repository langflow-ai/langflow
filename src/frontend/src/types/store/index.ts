export type storeComponent = {
  id: string;
  is_component: boolean;
  tags: string[];
  metadata?: {};
  downloads_count: number;
  name: string;
  description: string;
  liked_by_count: number;
};
