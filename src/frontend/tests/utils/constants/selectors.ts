/**
 * Re-export of selector constants so callers can import from a stable path
 * even if `testIds.ts` is renamed or split later.
 */
export { SELECTORS, TID } from "./testIds";
export { TEXTS } from "./texts";
export { ANIMATIONS, TIMEOUTS } from "./timeouts";
