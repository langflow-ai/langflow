"""Performance-suite flow fixtures and fixture_index helpers.

Package root for builders, validators, ``one_run``, and committed JSON under
``fixtures/``. Locust profiles and tests load fixtures from here after
``build_fixtures`` pins them.
"""
