exclude = [
    "demo_test_generation.py",
    "validation_test.py",
    "examples/",
]

[lint]
ignore = [
    "T201",  # Allow print statements in demo files
    "E402",  # Allow imports not at top in demo files
]