"""Lothal — an AI software builder built on Langflow.

All Lothal backend code lives under this namespace so it stays cleanly separated
from upstream Langflow (see the naming convention in the product backlog). Symbols
within the namespace use plain names (`Project`, `Message`, `CodeFile`).
"""
