# 「DB 驱动团队模板表」实施计划 - 1.0 版本

## 保存模版
在 工作流 设置好了之后，会在 页面上有一个导出json的按钮。
把导出json按钮的下面新增一个 保存为模版的按钮。
功能要包含导出json的功能中的生成json的部分，然后将这个json 保存到一张独立的表中，供所有人查看，
需要注意的是，清除所有的 密码，变量 以及 带有隐藏展示功能的字段的值。

然后把当前系统的 模版 做一个按钮出来，在页面上展示。
模版保留原来的逻辑，在加上现在这个表的数据。