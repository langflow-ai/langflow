apiVersion: v1
kind: Service
metadata:
  name: {{ include "genesis-studio-service.fullname" . }}
  labels:
    {{- include "genesis-studio-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "genesis-studio-service.selectorLabels" . | nindent 4 }}
