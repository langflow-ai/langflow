apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "genesis-studio-service.fullname" . }}-test-connection"
  labels:
    {{- include "genesis-studio-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "genesis-studio-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
