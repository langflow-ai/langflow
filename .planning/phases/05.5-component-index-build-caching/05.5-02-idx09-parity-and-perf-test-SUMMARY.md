---
phase: 05.5-component-index-build-caching
plan: 05.5-02-idx09-parity-and-perf-test
subsystem: component-index
tags: [test, cache-hit, parity, performance, IDX-09]
dependency_graph:
  requires: [IDX-08]
  provides: [IDX-09]
  affects: [get_and_cache_all_types_dict, component_cache.type_to_current_hash]
tech_stack:
  added: []
  patterns: [module-scoped fixture, tmp_path_factory, asyncio.run perf timing, _capture_parity_snapshot]
key_files:
  modified:
    - src/lfx/tests/unit/test_component_index.py
decisions:
  - "D-04 honored: parity test uses _capture_parity_snapshot against smallest.snapshot.json on both hit and miss paths"
  - "D-05 honored: sync def + asyncio.run() for perf test; only one event loop per call; ceiling 500ms"
  - "D-07 honored: telemetry.log_component_index.assert_not_called() on cache-hit"
  - "fixture scope: module-scoped prebuilt_cache_file uses tmp_path_factory + direct setattr (no monkeypatch) to avoid function-scope requirement"
  - "P-2 honored: assert ci.component_cache.type_to_current_hash is not None after cache-hit"
metrics:
  duration_minutes: 15
  tasks_completed: 3
  files_modified: 1
  completed_date: "2026-04-18T22:00:00Z"
---

# Phase 05.5 Plan 02: IDX-09 Parity and Perf Test Summary

**One-liner:** TestIDX08CacheHit class with five tests covering hit/miss behavior, telemetry skip, byte-identical parity via _capture_parity_snapshot, and a <500ms perf ceiling on the cache-hit path.

## What Was Built

Added a `prebuilt_cache_file` module-scoped pytest fixture and `TestIDX08CacheHit` class to `src/lfx/tests/unit/test_component_index.py`, directly after `TestIDX07StaleIndexWarning`.

### Fixture: `prebuilt_cache_file` (module-scoped)

Uses `tmp_path_factory` + direct `setattr` on `ci._get_cache_path` (restored via `finally`) to call `_save_generated_index` with a minimal synthetic `modules_dict`. Returns a `Path` to a real, SHA-stamped, version-matched cache file written once per module. Asserts the written blob has `version == _version("lfx")` and non-empty `entries` (both required for IDX-08 short-circuit per D-01).

### TestIDX08CacheHit (5 tests)

| Test | Path | Key Assertion |
|------|------|---------------|
| `test_cache_hit_populates_all_types_dict` | cache-hit | result non-None+non-empty, type_to_current_hash not None, import_langflow_components NOT called |
| `test_cache_miss_falls_back_to_rebuild` | cache-miss | result non-None+non-empty, import_langflow_components called exactly once |
| `test_cache_hit_skips_telemetry` | cache-hit | telemetry.log_component_index.assert_not_called() |
| `test_parity_cache_hit_vs_miss` | both | hit_snapshot == miss_snapshot == smallest.snapshot.json |
| `test_cache_hit_perf_under_500ms` | cache-hit | elapsed < 0.5s via time.perf_counter() around asyncio.run() |

## Tasks

| Task | Description | Commit | Result |
|------|-------------|--------|--------|
| 1 | prebuilt_cache_file fixture + hit/miss/telemetry tests | 78ae2b035a | 3 tests pass |
| 2 | Parity assertion via _capture_parity_snapshot (D-04) | 6c00cb3b1e | 1 test pass |
| 3 | Perf assertion <500ms (D-05) | d11675f745 | 1 test pass |

## Test Results

```
49 passed, 2 skipped in 1.56s
```

All 5 new `TestIDX08CacheHit` tests pass. The 2 skips are the existing OpenAI-dependent skips (`test_parity_five_types`, `test_parity_five_types_after_async_refactor`) that require `langchain_openai`, which is absent in the lfx-only test venv.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] PT018 compound assertion violations**
- **Found during:** Task 1 commit hook (ruff check)
- **Issue:** `assert isinstance(result, dict) and result, ...` and `assert result is not None and result, ...` violate ruff PT018 (assertion should be broken into multiple parts)
- **Fix:** Split each compound assertion into two single-condition asserts
- **Files modified:** src/lfx/tests/unit/test_component_index.py
- **Commit:** part of 78ae2b035a (re-staged after ruff fix)

**2. [Rule 1 - Style] ruff-format reformatted test_parity_cache_hit_vs_miss**
- **Found during:** Task 2 commit hook (ruff format)
- **Issue:** ruff-format adjusted whitespace/trailing comma in assert message strings
- **Fix:** Re-staged reformatted file
- **Commit:** 6c00cb3b1e

## Plan-Checker Items Resolved

1. **`prebuilt_cache_file` scope conflict:** Resolved by using `tmp_path_factory` + direct `setattr`/restore via `try/finally` instead of `monkeypatch` (which is function-scoped). The fixture is `scope="module"` as intended.

2. **`asyncio.run()` in asyncio_mode=auto file:** The perf test is a plain `def` (sync), not `async def`. In `asyncio_mode=auto`, sync test functions do not have a running event loop, so `asyncio.run()` creates its own loop safely. The other four tests are `async def` under `@pytest.mark.asyncio` on the class.

## Known Stubs

None. All five tests are fully wired to the IDX-08 short-circuit implementation from plan 05.5-01.

## Threat Flags

No new network endpoints, auth paths, file access patterns, or schema changes. Test-only changes.

## Self-Check: PASSED

- `src/lfx/tests/unit/test_component_index.py` exists and contains `TestIDX08CacheHit`
- Commits 78ae2b035a, 6c00cb3b1e, d11675f745 present in git log
- Full module: 49 passed, 2 skipped (expected)
- `TestIDX08CacheHit` suite: 5 passed
